#!/bin/bash -e
DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )
LIBS=$(echo $DIR/usr/lib/*linux*/)
LIBS=$LIBS:$(echo $DIR/usr/lib/*linux*/pulseaudio)
LIBS=$LIBS:$(echo $DIR/usr/lib/*linux*/blas)
exec ${DIR}/usr/lib/*/ld-*.so* --library-path $LIBS ${DIR}/usr/bin/ffmpeg "$@"
